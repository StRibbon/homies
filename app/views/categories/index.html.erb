<h1>Categories#index</h1>
<ul>

</ul>
<% @categories.each do |category|%>
<li style="color: <%= category.color %>;"><%= category.name %>
    <%= link_to "Edit", edit_category_path(category.id) %> 
</li>

<% end %>
</ul>
